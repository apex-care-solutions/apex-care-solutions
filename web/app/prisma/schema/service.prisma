
  model Service {
    name             String            @id
    description      String?
    createdAt        DateTime          @default(now())
    packageServices  PackageService[]
    job              Job[]
    technicianServices TechnicianService[]
  }
